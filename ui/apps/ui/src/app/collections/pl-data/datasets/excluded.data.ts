import { URL_PARAM_NAME } from '../../data/datasets/nav-config.data';
import { IExcludedFiltersConfig } from '../../repositories/types';

export const plExcludedDatasetsFilters: IExcludedFiltersConfig = {
  id: URL_PARAM_NAME,
  excluded: [],
};
