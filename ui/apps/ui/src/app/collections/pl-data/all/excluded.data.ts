import { URL_PARAM_NAME } from '../../data/all/nav-config.data';
import { IExcludedFiltersConfig } from '../../repositories/types';

export const plExcludedAllCollectionsFilters: IExcludedFiltersConfig = {
  id: URL_PARAM_NAME,
  excluded: [],
};
